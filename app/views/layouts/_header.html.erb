<nav class="navbar navbar-inverse navbar-fixed-top">
  <div class="container-fluid">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1">
        <span class="sr-only">MENU</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <a class="navbar-brand" href="<%= root_path %>">Pixly  <small id="app-spec">lite</small></a>

      <% if user_signed_in? %>
        <ul class="nav navbar-nav">
          <li><%= link_to "Share a photo", "#",  data: { toggle: "modal", target: "#upload_image_modal" } %></li>
          <li><a href="#" data-toggle="modal" data-target = "#invite_user_modal"><i class="fa fa-user-plus"></i></a></li>
        </ul>
      <% end %>

       <ul class="nav navbar-nav pull-right">
          <% @categories.each do |category| %>
            <li><%= link_to category.name, photos_path(filter: category.name), remote: true %></li>
          <% end %>
        </ul>
    </div>

    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
      <ul class="nav navbar-nav navbar-right">
        <% if user_signed_in? %>
          <li class="dropdown">
            <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-expanded="false"> 
              <span><i class="fa fa-user"> <%= current_user.email %> </i></span>
              <span class="caret"></span>
            </a>      
            <ul class="dropdown-menu" role="menu">

              <li><a href="#"><i class="fa fa-cog"></i><span>Settings</span></a></li>
              <li class="divider"></li>
              <li>
                <%= link_to signout_path, method: :delete do %>
                  <i class="fa fa-sign-out"></i>
                  <span>Sign out</span>
                <% end %>
              </li>
            </ul>
          </li>  
        <% else %>
          <li><%= link_to 'Login', login_path, class:"header-login" %></li>
        <% end %>             
      </ul>
    </div><!-- /.navbar-collapse -->
  </div><!-- /.container-fluid -->
</nav>